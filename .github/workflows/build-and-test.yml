name: Build iOS IPA (Sideloadable)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

env:
  BUILD_TYPE: Release

jobs:
  ios-build:
    name: iOS IPA Build
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      # ১. ডিপেনডেন্সি ইনস্টল করা (ব্যর্থ প্যাকেজটি বাদ দেওয়া হয়েছে)
      - name: Install Dependencies
        run: |
          brew update
          brew install cmake ninja boost glm ffmpeg sdl2 bullet freetype

      # ২. CMakeLists.txt ফাইলের বুস্ট কনফিগ প্যাচ করা (আগের মতো)
      - name: Patch CMakeLists for Boost
        run: |
          sed -i '' 's/find_package(Boost.*COMPONENTS.*/find_package(Boost REQUIRED)/g' CMakeLists.txt
          sed -i '' '/find_package(Boost REQUIRED)/a\
          include_directories(${Boost_INCLUDE_DIRS})\
          link_directories(${Boost_LIBRARY_DIRS})\
          ' CMakeLists.txt
          
          find . -name "CMakeLists.txt" -exec sed -i '' 's/Boost::program_options/boost_program_options/g' {} +
          find . -name "CMakeLists.txt" -exec sed -i '' 's/Boost::system/boost_system/g' {} +
          find . -name "CMakeLists.txt" -exec sed -i '' 's/Boost::filesystem/boost_filesystem/g' {} +

      # ৩. Xcode এবং iOS SDK ব্যবহার করে arm64 আর্কিটেকচারে কনফিগারেশন
      - name: Configure CMake for iOS
        run: |
          mkdir build
          cd build
          cmake .. -G Xcode \
            -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} \
            -DCMAKE_XCODE_ATTRIBUTE_CODE_SIGNING_ALLOWED=NO \
            -DCMAKE_XCODE_ATTRIBUTE_CODE_SIGN_IDENTITY="" \
            -DCMAKE_XCODE_ATTRIBUTE_DEVELOPMENT_TEAM=""

      # ৪. Xcode বিল্ড রান করা
      - name: Build App Using Xcode
        run: |
          cd build
          cmake --build . --config ${{ env.BUILD_TYPE }} -- -sdk iphoneos

      # ৫. বিল্ড হওয়া .app কে Sideloadable .ipa ফাইলে রূপান্তর করা
      - name: Package into Sideloadable .ipa
        run: |
          mkdir -p build/Payload
          
          # বিল্ড হওয়া মূল .app ডিরেক্টরি খুঁজে বের করে Payload-এ কপি করা
          APP_PATH=$(find build -name "*.app" -type d | head -n 1)
          echo "Found App Bundle at: $APP_PATH"
          
          if [ -z "$APP_PATH" ]; then
            echo "Error: No .app bundle found!"
            exit 1
          fi
          
          cp -r "$APP_PATH" build/Payload/
          
          # Payload ফোল্ডারটিকে জিপ করে .ipa তৈরি করা
          cd build
          zip -r openrw-sideload.ipa Payload/

      # ৬. Sideload করার জন্য প্রস্তুত .ipa ফাইলটি আপলোড করা
      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: openrw-ios-sideload
          path: build/openrw-sideload.ipa
